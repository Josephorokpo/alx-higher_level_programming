-- Task: Creates a table called first_table in the current database in your MySQL server.

-- Try to create the table. If it already exists, do nothing.
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
